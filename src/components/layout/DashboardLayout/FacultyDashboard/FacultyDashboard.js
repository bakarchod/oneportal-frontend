import React, { useState } from 'react';
import '../dashboard.css';
import { Link } from 'react-router-dom';
import Navbar from '../FacultyDashboard/components/Navbar';
const FacultyDashboard = (data) => {
    return (
        <div>
        <Navbar></Navbar>
    
    </div>
    );
}

export default FacultyDashboard;